# Examples

These files are intentionally small, reviewable contract examples.

- `workflow.yaml` demonstrates a bounded workflow manifest.
- `policy.yaml` demonstrates explicit capability and safety policy.
- `result.json` demonstrates evidence and verification output.

Examples are fixtures, not production credentials, deployment configurations, or guarantees of host behavior.
